Obtrace SDK integration chart installed.

Created resources:
  - Secret: {{ .Values.secret.name }} (if secret.create=true)
  - ConfigMap: {{ .Values.config.name }} (if config.create=true)
  - Optional demo Deployment: {{ .Values.workload.name }} (if workload.enabled=true)

For existing workloads, inject these env vars:
  - OBTRACE_API_KEY from Secret/{{ .Values.secret.name }}
  - OBTRACE_INGEST_BASE_URL from ConfigMap/{{ .Values.config.name }}
  - OBTRACE_TENANT_ID from ConfigMap/{{ .Values.config.name }}
  - OBTRACE_PROJECT_ID from ConfigMap/{{ .Values.config.name }}
  - OBTRACE_ENV from ConfigMap/{{ .Values.config.name }}
